import React from "react";
import "./Button.css";

const Button = (props) => {
    <div className="Button" data-testid="Button">
        Button Component
    </div>;
};

Button.propTypes = {};

Button.defaultProps = {};

export default Button;
